Ѕиблиотеки:

Ђiostream.hї - находитс€ в стандартной библиотеке €зыка и отвечает за ввод и вывод данных на экран
<cstdlib>    - стандартна€ библиотека €зыка —. Ёто подключение необходимо дл€ работы функции "system".


типы данных:

int Ч целочисленный тип данных (1 или 5 или 999).

char Ч символьный тип данных (a, b, c... or words abc, hello... ).

bool Ч логический тип данных. (False, true)
 лючевое слово типа bool Ч это псевдоним дл€ типа структуры System.Boolean .NET,
представл€ющий логическое значение: true или false.
ƒл€ выполнени€ логических операций со значени€ми типа bool
используйте логические операторы. “ип bool €вл€етс€ типом результата операторов сравнени€ и равенства.
¬ыражение bool может быть управл€ющим условным выражением 
в операторах if, do, while и for и условном операторе ?:.
«начение по умолчанию дл€ типа bool Ч false.

float Ч тип данных с плавающей зап€той (1.1 или 5.4 или 999.3).
double Ч тип данных с плавающей зап€той двойной точности.

»м€ double означает, что точность этих чисел вдвое превышает точность чисел типа float.
¬ большинстве случаев тип double €вл€етс€ наиболее удобным.
ќграниченной точности чисел float во многих случа€х попросту недостаточно. 
ѕричина, по которой тип float все еще используетс€, - экономи€ пам€ти при хранении 
(это важно дл€ больших массивов вещественных чисел).